# Custom CNN
This is a simple image classifier (Car brand classifier to be specific) written from scratch using the most basic PyTorch API.
It was implemented by leveraging only matrix multiplications and PyTorch's autograd implementation.
